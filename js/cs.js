var typed = new Typed('.typing', {
    // Waits 1000ms after typing "First"
    strings: [
        'Firewalls', 
        'Log Analysis'
    
    ],

    typeSpeed: 100,
    backSpeed: 100,
    loop: true,

  });